version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd vidya-setu
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: vidya-setu/dist
    files:
      - '**/*'
  cache:
    paths:
      - vidya-setu/node_modules/**/*
